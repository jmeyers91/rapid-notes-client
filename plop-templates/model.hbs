import Model from '@simplej/mobx-model';

export default class {{properCase name}} extends Model {
  static get schema() {
    return {
      id: Number,
      createdAt: Date,
      updatedAt: Date,
    };
  }
}

